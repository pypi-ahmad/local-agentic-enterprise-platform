# FAQ

## Is internet required for inference?
No. Core inference runs locally through Ollama.

## Can I disable Google OAuth?
Yes. Local auth with break-glass account works without OAuth.

## How do I prevent risky actions?
Approval gates are mandatory for destructive SQL and sensitive workflows.

## Can this run on CPU only?
Yes. It is slower, but routing falls back automatically.
